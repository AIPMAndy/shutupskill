# shutupskill

让 AI 不废话 — 通过提示词优化改善输出风格。

## 问题

GPT-5.4/Kimi 在 OpenClaw 中的通病：
- ❌ **啰嗦** — 一句能说完的事，输出 200+ Token
- ❌ **工具调用弱** — 参数缺失、串行低效
- ❌ **半途而废** — 遇错就停，推给用户
- ❌ **Token 浪费** — 冠词、客套话、犹豫表达，全是无效消耗

## 解决

通过提示词优化，改变 AI 输出风格和工作流程：

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 单次输出 Token | 200-500 | **50-150**（降 60-70%） |
| 输出风格 | 啰嗦、客套 | **简洁、直接** |
| 工作流程 | 问完再做 | **先做后说** |
| 输出句数 | 5-10 句 | **≤ 3 句** |

## 为什么能省 Token？

废话 = 浪费的 Token = 浪费的钱。

```
优化前（178 Token）：
"好的，让我先帮您读取一下这个配置文件，然后我会仔细分析其中
可能存在的性能瓶颈问题。我注意到这个文件有 100 行代码，其中
包含了一些可能影响性能的部分。您希望我优先优化哪个方面呢？"

优化后（31 Token）：
[直接执行]
"配置有 3 处瓶颈。已优化。响应时间降 60%。"
```

**每次对话省 60-70% Token，长期累积效果显著。**

## 核心优化

不废话 ≠ 功能缩水。通过提示词引导：

- **先做后说** — 直接执行，不问"您希望怎样"
- **工具并行** — 能同时调用的工具不串行，提升效率
- **错误重试** — 引导模型遇错尝试多种方案
- **输出规范** — 明确的输出格式和禁用词规则

**原理：通过结构化提示词，引导模型改变输出风格和工作流程。**

## 功能

通过在 agent 的 SOUL.md 中注入优化规则，引导 AI 改变输出风格：
- 先做后说，减少询问
- 工具并行调用提示
- 错误重试引导
- 极简输出规范

## 安装

```bash
openclaw skill install https://github.com/AIPMAndy/shutupskill
```

手动：

```bash
cd ~/.openclaw/skills
git clone https://github.com/AIPMAndy/shutupskill
cd shutupskill && npm install
```

## 使用

```bash
/shutup --all                    # 优化所有 agent
/shutup --agent dev              # 优化指定 agent
/shutup --template-only          # 查看优化模板
/shutup --restore                # 恢复所有备份
/shutup --restore --agent dev    # 恢复指定 agent
```

## 效果对比

### 优化前
```
用户："优化 API"

AI："好的，让我先读取文件，然后分析性能瓶颈..."    ← 废话
"您希望优先优化哪个？"                              ← 推给用户
```

### 优化后
```
用户："优化 API"

AI：[直接执行]                                      ← 零废话
"发现 3 个问题，优化中。"                            ← 状态更新
"完成。响应时间：850ms → 290ms。"                    ← 结果 + 数据
```

**Token 消耗：178 → 31。省了 83%。**

## 工作原理

这是一个**提示词注入工具**，通过在 `~/.openclaw/agents/<agent>/workspace/SOUL.md` 末尾追加优化规则来改变 AI 行为。

**能做到的**：
- ✅ 改变输出风格（少废话）
- ✅ 引导工作流程（先做后说）
- ✅ 提供检查清单

**做不到的**：
- ❌ 保证任务完成率
- ❌ 强制工具精准度
- ❌ 让弱模型变成强模型

**效果取决于模型本身的能力。**

## 优化维度

| 维度 | 规则 | 效果 |
|------|------|------|
| 工作流程 | 先做后说，不问再做 | 减少来回对话轮次 |
| 工具调用 | 检查清单 + 优先级 + 并行提示 | 引导更好的工具使用 |
| 输出效率 | ≤ 3 句话 + 删冠词/客套话 | **输出 Token 降 60-70%** |
| 错误处理 | 引导尝试多种方案 | 减少直接放弃的情况 |

### 输出规则

**删除（省 Token）：**
- 冠词（"一个"、"这个"）
- 语气填充词（"那么"、"其实"）
- 客套话（"希望能帮到您"）
- 犹豫性表达（"可能"、"也许"）

**保留（保质量）：**
- 技术术语、代码块、报错信息
- 短句、碎片句

**优先短同义词：**
- 「大」不是「庞大」 — 省 1 Token
- 「修」不是「实施解决方案」 — 省 3 Token
- 「查」不是「进行检查」 — 省 2 Token

**推荐句式：**
```
[问题][动作][原因]。[下一步]。

"配置错误。已修复。原因：缺 API key。重启生效。"
"性能瓶颈在第 45 行。已优化。响应时间降 60%。"
```

## Token 节省估算

| 场景 | 优化前 | 优化后 | 节省 |
|------|--------|--------|------|
| 单次回复 | ~300 Token | ~100 Token | 67% |
| 10 轮对话 | ~3000 Token | ~1000 Token | 67% |
| 日均 100 轮 | ~30K Token | ~10K Token | **20K Token/天** |
| 月度（按 GPT-5.4 $10/1M Token） | $9 | $3 | **$6/月** |

## 兼容性

- OpenClaw: >= 2026.3.28
- 模型: GPT-5.x, Kimi, Claude, Qwen, MiniMax

## 卸载

```bash
openclaw skill uninstall shutupskill
/shutup --restore
```

## 许可证

MIT

## 作者

Andy
